# Autonomous SRE Agent (working title TBD)

An agent that watches a running system, detects incidents, diagnoses root cause from logs +
metrics, and recommends fixes with an eval harness that scores it against injected-fault truth. 


# Top-level tooling for the agent + eval layers (later days).
# The system-under-observation has its own pinned deps in system/requirements.txt.
anthropic>=0.40
python-dotenv>=1.0
pyyaml>=6.0
pytest>=8.0
requests>=2.31
